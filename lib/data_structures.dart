class Place {
  String name;
  int distance;
  List contact;
  Map address;
  Map position;
  String id;
  Place(this.name, this.distance, this.contact, this.address, this.position,
      this.id);
}

class User {
  String name;
  User(this.name);
}
